//Main Action to load


function dreamServ {

    
    this.initializeServer = function () {
        const DATA_ACCESS = require("dataAccess.js");               ///ccbHTTP-Request Layer
        const ACTION = requrie("action_dw98online.js");             ///main ccbAction for Initializing the server in CC
        

        const MODEL_PLAYER = require("models/Player.js");           ///Model for Player
        const MODEL_MAP = require("models/map.js");                 ///Model for Map
    }
    this.stopServer() = function() {

    }
}

var runServer = function () {
    dreamServ.initializeServer;
}